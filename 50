;; 50 Split by type

Takes seq, returns set of subseqs sorted by type,
maintaining internal order

(comp vals (partial group-by type))

#(vals (group-by type %))

(fn [coll] (into #{} (map last (group-by class coll))))

user=> (set ((fn [coll]
               (into #{} (map last (group-by class coll))))
             [1 :a 2 :b 3 :c]))
#{[:a :b :c] [1 2 3]}